{
  "name": "addiction",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server 'src' --files 'src' --ss  --reload-debounce 500",
    "start:mobile": "browser-sync start --server 'src' --files 'src' --ss  --reload-debounce 500 --browser Safari"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.3"
  }
}
